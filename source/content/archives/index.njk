---
eleventyExcludeFromCollections: true
title: "Archives"
permalink: "archives/"
---
{% extends "../../layouts/page.njk" %}

{% block content %}
  <h1>{{ title }}</h1>

  {% if collections.onThisDay | length > 0 %}
  <h2>On this day {{ collections.onThisDay[0].date | date('Do MMM') }}</h2>
  <ul>
  {% for post in collections.onThisDay %}
    <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
  {% endfor %}
  </ul>
  {% endif %}

  <h2>Explore content by year</h2>
  <ol>
  {% for year in collections.years %}
    <li><a href="/archives/{{ year }}">{{ year }}</a></li>
  {% endfor %}
  </ol>

  <h2>Explore content by tag</h2>
  <ul>
  {% for tag in collections.tags %}
    <li><a href="/tags/{{ tag.title | slug }}">{{ tag.title | title }}</a> - {{ tag.count }} items</li>
  {% endfor %}
  </ul>
{% endblock %}