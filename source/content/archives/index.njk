---
eleventyExcludeFromCollections: true
title: "Archives"
permalink: "/archives/"
---
{% extends "../../layouts/page.njk" %}

{% block title %}
  <h1>{{ title }}</h1>
{% endblock %}

{% block content %}
<div class="container">
  <h2>Explore my content by day</h2>
  <div class="prose">
  {% for day, data in collections.archives %}
    <h2>{{ day }}: {{ data.day}} {{ data.month }}</h2>
    <ul>
      {% for post in data.posts %}<li><a href="{{ post.url }}">{{ post.data.title }}</a></li>{% endfor %}
    </ul>
  {% endfor %}
  </p>

  {# <h2 class="explore-heading">Explore my content by year</h2>
  {{ collections.years | dump(2) }}
  <ol class="explore">
  {% for year in collections.years %}
    <li><h3><a href="/archives/{{ year }}">{{ year }}</a></h3></li>
  {% endfor %}
  </ol> #}

  <h2 class="explore-heading">Explore my content by tag</h2>
  <ul class="explore">
  {% for tag in collections.tags %}
    <li>
      <a href="/tags/{{ tag.title | slug }}">
        <h3>{{ tag.title | title }}</h3>
        <span class="meta">{{ tag.count }} {{ 'item' | plural(tag.count) }}</span>
      </a>
     </li>
  {% endfor %}
  </ul>
</div>
{% endblock %}